/* ================================
Week 6 Assignment: Midterm Functions + Signatures
================================ */

// Get specimen data
var specimenData = ???

// Get state boundary data
var stateData = ???

// Join the specimen data to the state data
var joinData = function(specimenData, stateData) {};

// Find the species with the most specimens collected in the selected state
var maxSpecies = function(joinData, selectedState) {};

// Find the collector who have collected the most specimens in the selected state
var maxCollector = function(joinData, selectedState) {};

// Find the total number of specimens collected in the selected state
var totalSpecimens = function(joinData, selectedState) {};

// Filter the specimen data with the collector of the specimens
var filterCollector = function(specimenData, userinputCollector) {};

// Filter the specimen data with the species of the specimens
var filterSpecies = function(specimenData, userinputSpecies) {};

// Filter the specimen data with the state where the specimens were collected
var filterState = function(specimenData, userinputState) {};
